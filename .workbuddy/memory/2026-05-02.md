# 2026-05-02

## applist.conf 常用APP覆盖完善
- 用户要求完善 applist.conf 以覆盖市面上基本所有常用APP，并参考骁龙8至尊版线程优化
- 搜索了骁龙8 Elite Gen 5 的 CPU 架构：2+6全大核（CPU0-1超大核4.6GHz + CPU2-7性能核3.62GHz），无小核设计
- 在原有350条规则基础上新增约640条规则，总计990条活跃规则
- 新增覆盖17个类别：社交、视频、音乐、购物、外卖、出行、银行、阅读、新闻、浏览器、输入法、云存储、工具、求职、房产、游戏等
- 新增骁龙8至尊版线程优化策略说明注释（针对2+6全大核架构适配）
- APP线程分配策略：主线程/UI → 3-5，binder → 0-2，默认 → 0-2；游戏主线程/渲染 → 6-7
- 配置文件从738行扩展到1603行

## AppOpt 模块 Bug 修复 & Web 界面优化
### Bug 修复
1. `customize.sh`：删除未定义的 `remove_sys_perf_config` 函数调用
2. `customize.sh`：修复 `check_rule_exist` 中 `grep -F "^$PKG_NAME"` 导致防重复检测完全失效的问题，改为 `grep -q "^${PKG_NAME}[={]"`
3. `customize.sh`：修复 `pkg_exists` 子串匹配误判，同样改为行首精确匹配
4. `applist.conf`：删除 Azur Lane 空壳条目（只有注释没有规则）
5. `applist.conf`：Roblox 和 Minecraft 改用「无UnityMain」游戏模板（自研引擎非Unity）
6. `service.sh`：变量路径 `MODDIR` 加双引号增强健壮性
7. 创建 `uninstall.sh` 卸载脚本，清理运行时日志和临时文件
### Web 界面优化
- 修复 `.bak-item` CSS bug（`border-radius: 1px solid` → `border: 1px solid`）
- 添加状态指示灯脉冲动画（绿色运行/红色异常）
- Modal 弹窗添加底部滑入动画
- Tab 切换添加滑动指示器动画
- 列表项交错入场动画、规则卡片入场动画
- 按钮按压增强反馈（scale + filter brightness）
- 搜索框添加清除按钮（✕）
- Toast 弹出动画优化为 cubic-bezier
- 信息页卡片交错入场动画
- 标题渐变色流动动画
- 空状态图标弹跳动画
- 帮助提示块添加左侧主题色边框
- 全局平滑滚动
- 悬停/选中增强阴影反馈

### 深度美化（第二轮）
- 信息页全面重构：Hero 渐变横幅 + 4格统计卡片网格 + 卡片式帮助说明区域
- 核心绑定说明用红/蓝/绿三色标签区分高/中/低核心
- 标题渐变升级为四色流动（indigo→violet→teal→indigo）
- Modal 顶部添加拖拽手柄指示条
- 标签添加微光边框（box-shadow ring）
- 按钮添加彩色阴影（bok/berr/bwarn/bp 各带对应色阴影）
- 规则核心标签代码风格增强（monospace + 渐变背景 + 边框）
- body 背景添加微妙装饰性径向渐变
- 标题行新增版本徽章（v8）

## 补充遗漏APP规则
- 补充 8 个遗漏的常见APP：QQ浏览器、快影、小米应用商店、Google Play、YouTube Music、Steam、虎扑、汽车之家
- 唯一包名总数从 442 增至 450 个

## 上传 GitHub
- GitHub 用户名：Z-Fovik
- 仓库地址：https://github.com/Z-Fovik/AppOpt
- 使用 Classic Personal Access Token（ghp_EPK...）认证推送
- 首次推送需要设置系统代理 http://127.0.0.1:7897
- 推送完成后已清理远程 URL 中的 token 和临时代理配置
